<div class="wrapper">
  <h1>Book Communication</h1>
</div>
<div class="button-zone">
  本の感想を登録する
</div>
<div class="main-field">
  <%= form_with model: @book, local: true do |f| %>
    <div class="form-style">
      <div class="form-space">
      <%# 本のタイトル %>
        <%= f.label :title, class:"user-form-tag" %>
        <%= f.text_field :title, class:"user-form-field" %>
      </div>
      
      <%# 本の画像 %>
      <div class="img-upload">
        <%= f.label :image, class:"user-form-tag" %>
        <div class="click-upload">
          <p>
            クリックしてファイルをアップロード
          </p>
          <%= f.file_field :image, id:"item-image" %>
        </div>
      </div>

      <%# 本の作者 %>
      <div class="form-space">
        <%= f.label :author, class:"user-form-tag" %>
        <%= f.text_field :author, class:"user-form-field" %>
      </div>

      <%# 読んだ感想 %>
      <div class="form-space">
        <%= f.label :impression, class:"user-form-tag" %>
        <%= f.text_area :impression, class:"impression-form-field" %>
      </div>

      <div class="btn-area">
        <%= f.submit "登録する", class:"btn" %>
        <%= link_to "戻る", user_path(current_user.id), class:"btn-2" %>
      </div>
    </div>
  <% end %>
</div>